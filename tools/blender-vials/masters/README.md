# Approved masters

Approved **1024 × 1536** unlabeled plates live here. `publish-stocks.mjs`
reads this folder, not `renders/` or `references/`.

Promote a final plate only after Benjamin approves the look:

```bash
cp tools/blender-vials/renders/01_3mL_White_Powder_LOCKED.png \
   tools/blender-vials/masters/
```

Do not put previews, image-gen references, or labeled marble shots here.
5 mL (`05_5mL_White_Powder_LOCKED.png`) stays studio-only until a `5ML`
placement profile exists.
